<div align="center">
<img width="1200" height="475" alt="SmartSpeek AI Banner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# 🚀 SmartSpeek AI: IT English School

**Адаптивная AI-школа английского языка для IT и AI специалистов**

SmartSpeek AI — это инновационная платформа для изучения профессионального английского языка, специально разработанная для IT-профессионалов. Приложение использует Google Gemini AI для создания персонализированных программ обучения, адаптированных под вашу роль, уровень и карьерные цели.

## 📋 Содержание

- [Особенности](#особенности)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Статус проекта](#статус-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Конфигурация](#конфигурация)
- [Основные компоненты](#основные-компоненты)
- [Архитектура](#архитектура)
- [API и сервисы](#api-и-сервисы)
- [Разработка](#разработка)
- [Деплой](#деплой)
- [Roadmap](#roadmap)

## ✨ Особенности

### 🎯 Персонализация через AI
- **Умный онбординг**: Система собирает информацию о вашей роли, уровне английского, целях и рабочих задачах
- **Нейро-Оркестратор**: Персональный AI-наставник с уникальной манерой общения, адаптированной под ваш профиль
- **Адаптивные планы обучения**: 7-дневные и 4-недельные программы, генерируемые на основе ваших данных

### 📚 Контент для IT-специалистов
- **Специализированные курсы**: 
  - Аббревиатуры AI и IT (LLM, NLP, ML, DL, RAG и др.)
  - DevOps Communication (инциденты, масштабирование, CI/CD)
- **Практические сценарии**: Daily standup, code review, технические дискуссии, собеседования
- **IT-терминология**: Интерактивный словарь с объяснениями

### 🎮 Геймификация
- **Система достижений**: 23+ достижений с 7 уровнями сложности
- **Стрики**: Отслеживание ежедневной активности
- **Skill Tree**: Визуализация прогресса по навыкам (Grammar, Speaking, Vocabulary, Listening)

### 📊 Аналитика и прогресс
- **Dashboard**: Обзор активных курсов, выученных слов, текущего уровня
- **Детальная аналитика**: Прогресс по курсам, навыкам и времени обучения
- **AI Insights**: Персонализированные советы от BuddyAgent

### 🔄 Offline-First подход
- Работа без интернета (ограниченный функционал)
- Синхронизация данных при восстановлении соединения
- Локальное хранение прогресса в localStorage

### 📱 Адаптивный дизайн
- Полностью responsive интерфейс
- Мобильная версия с выдвижной боковой панелью
- Современный UI с анимациями и переходами

## 🛠 Технологический стек

### Frontend
- **React 19.2.3** — UI библиотека
- **TypeScript 5.8.2** — типизация
- **Vite 6.2.0** — сборщик и dev-сервер
- **Tailwind CSS** — стилизация (через CDN)
- **D3.js 7.9.0** — визуализация данных (Skill Tree)
- **Font Awesome 6.4.0** — иконки

### AI и Backend
- **Google Gemini AI (gemini-3-flash-preview)** — генерация персонализированных планов и инсайтов
- **@google/genai 1.35.0** — SDK для работы с Gemini API

### Инструменты разработки
- **@vitejs/plugin-react** — плагин для React
- **@types/node** — типы для Node.js

## 📁 Структура проекта

```
smartspeek-ai_-it-english-school/
├── api/                     # FastAPI backend
│   ├── app/
│   │   ├── agents/             # Orchestrator agents + prompts
│   │   ├── routers/            # API эндпоинты
│   │   ├── config.py           # ENV настройки
│   │   ├── db.py               # SQLAlchemy база
│   │   ├── models.py           # Модели БД
│   │   ├── schemas.py          # Pydantic схемы
│   │   └── seed.py             # Seed контента
│   ├── alembic/              # Миграции
│   ├── alembic.ini           # Конфиг Alembic
│   ├── main.py               # Точка входа FastAPI
│   └── requirements.txt      # Python зависимости
├── components/              # React компоненты
│   ├── AchievementBadge.tsx    # Бейдж достижения
│   ├── AITerminology.tsx        # Словарь IT-терминов
│   ├── CourseCard.tsx           # Карточка курса
│   ├── OrchestratorPanel.tsx    # Панель нейро-оркестратора
│   ├── Sidebar.tsx              # Боковая навигация
│   ├── SkillTree.tsx            # Дерево навыков (D3.js)
│   └── Views.tsx                # Все основные представления
├── services/                 # Сервисы и API
│   └── gemini.ts                # Интеграция с Gemini AI
├── App.tsx                   # Главный компонент приложения
├── constants.ts              # Константы и мок-данные
├── types.ts                  # TypeScript типы и интерфейсы
├── index.tsx                 # Точка входа
├── index.html                # HTML шаблон
├── vite.config.ts            # Конфигурация Vite
├── tsconfig.json             # Конфигурация TypeScript
├── package.json              # Зависимости и скрипты
└── metadata.json             # Метаданные приложения
```

## ✅ Статус проекта

### ✅ Выполнено
- ✅ FastAPI backend в `/api` с роутами, схемами, моделями и CORS
- ✅ PostgreSQL + Alembic миграции + seed (курсы/уроки/достижения)
- ✅ API эндпоинты `/api/courses`, `/api/lessons`, `/api/dashboard`, `/api/progress`
- ✅ Orchestrator (Guard → Router → Agent → Guard) + единый JSON контракт
- ✅ Telegram webhook и базовый поток практики (MCQ)
- ✅ Frontend подключен к API без изменения UI (данные курсов/уроков/прогресса)
- ✅ Регистрация и логин пользователей (`/api/auth/register`, `/api/auth/login`)
- ✅ Docker конфигурация для развертывания
- ✅ GitHub репозиторий настроен и защищенные ветки настроены
- ✅ README дополнен инструкциями по Postgres/Alembic/seed/запуску

### ⚠️ Нужно выполнить до запуска
> 📋 **Подробный список**: См. [LAUNCH_CHECKLIST.md](./LAUNCH_CHECKLIST.md)

**Критически важно:**
- Интеграция JWT токенов в API (сейчас используется `X-User-Id`)
- Сохранение онбординга и настроек в БД
- Тестирование всех API эндпоинтов
- Проверка работы seed скрипта
- Создание `.env.example`

**Желательно:**
- Убрать дублирование данных из `constants.ts`
- Улучшить обработку ошибок
- Протестировать Telegram интеграцию
- Протестировать Orchestrator агентов

## 🚀 Установка и запуск

### Предварительные требования

- **Node.js** (рекомендуется версия 18+)
- **npm** или **yarn**
- **Python 3.10+**
- **PostgreSQL 14+**
- **Google Gemini API Key** ([получить здесь](https://makersuite.google.com/app/apikey))

### Шаги установки

1. **Клонируйте репозиторий** (если еще не склонирован):
```bash
git clone <repository-url>
cd smartspeek-ai_-it-english-school
```

2. **Установите зависимости**:
```bash
npm install
```

3. **Настройте переменные окружения**:
   
   Создайте файл `.env.local` в корне проекта:
```bash
GEMINI_API_KEY=your_api_key_here
```

   ⚠️ **Важно**: Не коммитьте `.env.local` в репозиторий! Добавьте его в `.gitignore`.

4. **Запустите dev-сервер**:
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### Backend (FastAPI)

1. **Установите Python зависимости**:
```bash
cd api
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2. **Создайте `.env` для backend (не коммитить)**:
```bash
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/smartspeek
LLM_API_KEY=your_llm_api_key
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_WEBHOOK_SECRET=
```

> Временный режим SQLite (если Postgres/psycopg2 не ставится):
```bash
cd api
pip install -r requirements.sqlite.txt
```
и в `.env`:
```
DATABASE_URL=sqlite:///./smartspeek.db
```

3. **Поднимите PostgreSQL локально** (пример через Docker):
```bash
docker run --name smartspeek-db -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=smartspeek -p 5432:5432 -d postgres:14
```

4. **Примените миграции Alembic**:
```bash
cd api
alembic upgrade head
```

5. **Выполните seed**:
```bash
cd api
python -m app.seed
```

6. **Запустите FastAPI**:
```bash
cd api
uvicorn main:app --reload --port 8000
```

### Запуск FastAPI + Vite вместе

Откройте два терминала:
```bash
# Терминал 1
cd api
uvicorn main:app --reload --port 8000

# Терминал 2
npm run dev
```

### Docker (быстрый запуск для тестирования)

```bash
docker compose up --build
```

Что поднимется:
- Frontend: `http://localhost:3000`
- Backend: `http://localhost:8000`
- Postgres: `localhost:5432`

Переменные окружения можно передать через `.env`:
```
GEMINI_API_KEY=ваш_ключ
GEMINI_MODEL=gemini-2.5-flash
LLM_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
```

### Telegram webhook

1. Запустите API на публичном URL (например, через ngrok):  
2. Установите webhook:
```bash
curl -X POST "https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook" \\
  -d "url=https://your-public-url/api/telegram/webhook"
```

### Другие команды

```bash
# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная |
|-----------|----------|--------------|
| `GEMINI_API_KEY` | API ключ для Google Gemini | Да |
| `DATABASE_URL` | URL подключения к PostgreSQL | Да (backend) |
| `LLM_API_KEY` | Ключ LLM (если будет подключен) | Нет |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Нет |
| `TELEGRAM_WEBHOOK_SECRET` | Секрет вебхука Telegram | Нет |
| `VITE_API_BASE_URL` | Base URL API для фронта | Нет |

### Vite конфигурация

Сервер разработки настроен на:
- **Порт**: 3000
- **Хост**: 0.0.0.0 (доступен извне)

Переменные окружения автоматически подхватываются через `loadEnv` и доступны в коде как `process.env.GEMINI_API_KEY`.

## 🧩 Основные компоненты

### App.tsx
Главный компонент, управляющий:
- Фазами приложения (welcome → auth → onboarding → main)
- Текущим представлением (dashboard, courses, analytics и т.д.)
- Состоянием пользователя и онбординга
- Синхронизацией с localStorage
- Обработкой онлайн/оффлайн режимов

### Views.tsx
Содержит все основные представления:
- `WelcomeView` — приветственный экран
- `AuthView` — регистрация/выбор аватара
- `OnboardingSurveyView` — опрос для персонализации
- `GeneratingPlanView` — экран генерации плана
- `PlanReadyView` — отображение готового плана
- `CoursesView` — список курсов
- `AnalyticsView` — аналитика прогресса
- `DailySessionView` — практическое занятие
- `LearningPlanView` — детальный план обучения
- `SettingsView` — настройки
- `TelegramView` — интеграция с Telegram (заглушка)

### Sidebar.tsx
Боковая панель навигации с разделами:
- Dashboard
- Курсы
- Учебный план
- Аналитика
- Достижения
- Telegram
- Настройки

### OrchestratorPanel.tsx
Панель нейро-оркестратора — персонального AI-наставника:
- Отображение персональности (tone, description)
- Задачи и прогресс
- Интерактивные подсказки

### SkillTree.tsx
Визуализация навыков с помощью D3.js:
- Иерархическая структура навыков
- Интерактивное дерево с анимациями

## 🏗 Архитектура

### Поток данных

```
User Input → React Components → State Management (useState/useEffect)
                                    ↓
                            Services (gemini.ts)
                                    ↓
                            Google Gemini API
                                    ↓
                            Response Processing
                                    ↓
                            State Update → UI Re-render
```

### Хранение данных

- **Локальное хранение**: `localStorage` для:
  - Данных пользователя (`smartspeek_user`)
  - Результатов онбординга (`smartspeek_onboarding`)
  - Текущей фазы (`smartspeek_phase`)

- **Серверное хранение**: PostgreSQL (курсы, уроки, попытки, достижения, логи оркестратора)

### Состояние приложения

Приложение использует локальное состояние React:
- `useState` для компонентного состояния
- `useEffect` для side effects и синхронизации
- `useCallback` для мемоизации функций

## 🔌 API и сервисы

### services/gemini.ts

#### `processOnboarding(onboardingData: OnboardingData)`
Генерирует персонализированный план обучения на основе данных онбординга.

**Входные данные:**
- Роль пользователя
- Уровень английского
- Цели обучения
- Рабочие задачи
- И другие параметры

**Возвращает:**
```typescript
{
  persona: {
    tone: string,
    correction_level: string,
    speech_speed: string,
    description: string
  },
  learning_plan_summary: string,
  learning_plan_id: string,
  first_lesson: {
    topic: string,
    description: string
  }
}
```

#### `getTutorInsights(prompt: string)`
Генерирует персонализированные советы от AI-наставника.

**Возвращает:**
```typescript
Array<{
  topic: string,
  message: string,
  priority: 'low' | 'medium' | 'high'
}>
```

#### `evaluateDiagnosticResponse(stepType, question, answer)`
Оценивает ответ пользователя в диагностических заданиях.

**Возвращает:**
```typescript
{
  score: number,
  feedback: string,
  cefr_hint: string
}
```

## 💻 Разработка

### Стиль кода

- **TypeScript**: Строгая типизация
- **Компоненты**: Functional Components с хуками
- **Стили**: Tailwind CSS utility classes
- **Именование**: camelCase для переменных, PascalCase для компонентов

### Добавление нового курса

1. Добавьте данные курса в `constants.ts`:
```typescript
export const COURSES_DATA: Course[] = [
  // ... существующие курсы
  {
    id: 'new_course_id',
    name: 'Название курса',
    description: 'Описание',
    progress: 0,
    totalLessons: 5,
    completedLessons: 0,
    color: '#2563EB',
    icon: 'fa-icon',
    lessons: [/* уроки */]
  }
];
```

2. Уроки автоматически отобразятся в `CoursesView`.

### Добавление нового достижения

Добавьте в массив `ACHIEVEMENTS` в `constants.ts`:
```typescript
{
  id: 'new_achievement',
  tier: 1-7,
  title: 'Название',
  description: 'Описание',
  icon: 'fa-icon',
  status: 'locked' | 'unlocked',
  type: 'course' | 'streak' | 'skill',
  date?: string
}
```

## 🌐 Деплой

### Production сборка

```bash
npm run build
```

Артефакты будут в папке `dist/`.

### Рекомендуемые платформы

- **Vercel** — оптимально для React приложений
- **Netlify** — простой деплой с поддержкой переменных окружения
- **GitHub Pages** — для статического хостинга
- **Cloudflare Pages** — быстрый CDN

### Настройка переменных окружения на хостинге

Убедитесь, что `GEMINI_API_KEY` настроен в настройках вашего хостинга.

## ⚠️ Что осталось для запуска

> 📋 **Подробный чеклист**: См. [LAUNCH_CHECKLIST.md](./LAUNCH_CHECKLIST.md)

### 🔴 Критически важно (обязательно перед запуском)

1. **Интеграция токенов в API**
   - Регистрация/логин работают, но API все еще принимает `X-User-Id` напрямую
   - Нужно добавить JWT токены и обновить `apiClient.ts` и `common.py`
   - **Файлы**: `api/app/routers/common.py`, `services/apiClient.ts`

2. **Сохранение данных пользователя**
   - Сохранение результата онбординга в БД (`user_profile.preferences_json`)
   - Сохранение настроек (имя, CEFR уровень) в БД
   - API endpoint `PUT /api/user/profile` для обновления профиля
   - **Файлы**: `App.tsx`, `components/Views.tsx` (SettingsView)

3. **Проверка работы всех API эндпоинтов**
   - Тестирование всех endpoints с реальной БД
   - Проверка сохранения попыток прохождения уроков
   - Проверка работы Orchestrator агентов

4. **База данных и seed**
   - Проверить выполнение seed скрипта без ошибок
   - Убедиться, что все 5 курсов с уроками загружены

5. **Переменные окружения**
   - Создать `.env.example` с примерами всех переменных
   - Проверить документацию по настройке

### 🟡 Важно для стабильности (желательно перед запуском)

6. **Убрать дублирование данных**
   - Удалить старые данные из `constants.ts` (COURSES_DATA, ACHIEVEMENTS)
   - Полностью перейти на данные из API

7. **Обработка ошибок**
   - Понятные сообщения об ошибках для пользователя
   - Fallback при недоступности Gemini API

8. **Telegram интеграция**
   - Проверить работу "Практика" - выдача MCQ и сохранение в БД
   - Проверить синхронизацию прогресса между Telegram и Web

9. **Тестирование Orchestrator агентов**
   - Проверить что все агенты возвращают правильный JSON
   - Проверить GuardAgent - блокирует инъекции

### 🟢 Желательно (можно после запуска)

10. **Тестирование** (unit/integration/E2E)
11. **Логирование и мониторинг** (Sentry/analytics)
12. **Безопасность**: rate limiting, CSP, proxy для LLM ключей
13. **Документация API** (OpenAPI/Swagger)
14. **CI/CD** (автотесты и деплой)

## 📈 Roadmap

### Текущая версия (v0.0.0)
- ✅ Базовый онбординг
- ✅ Интеграция с Gemini AI
- ✅ Система курсов
- ✅ Достижения и геймификация
- ✅ Offline-first подход

### Планируемые улучшения
- [x] Backend API для хранения данных
- [x] Регистрация и логин пользователей
- [ ] Интеграция JWT токенов в API
- [x] База данных для прогресса обучения
- [x] Интеграция с Telegram ботом (минимальный поток)
- [x] Docker конфигурация для развертывания
- [ ] Голосовое взаимодействие (speech-to-text, text-to-speech)
- [ ] Система оплаты и подписок
- [ ] Расширенная аналитика
- [ ] Социальные функции (лидерборды, сообщества)
- [ ] Мобильное приложение (React Native)

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

[Укажите лицензию проекта]

## 📧 Контакты

[Укажите контактную информацию]

---

**Сделано с ❤️ для IT-сообщества**
