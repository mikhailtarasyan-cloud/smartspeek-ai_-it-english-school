# 🚀 Чеклист готовности к запуску

## ✅ Критически важно (обязательно перед запуском)

### 1. Аутентификация и авторизация
- [x] Регистрация пользователей (`/api/auth/register`)
- [x] Логин пользователей (`/api/auth/login`)
- [ ] **Интеграция токенов/JWT в API** - сейчас API принимает `X-User-Id` напрямую
- [ ] **Обновить `apiClient.ts`** - использовать токены вместо `X-User-Id`
- [ ] **Обновить `common.py`** - проверять токены вместо заголовка
- **Файлы**: `api/app/routers/common.py`, `services/apiClient.ts`, `api/app/routers/auth.py`

### 2. Сохранение данных пользователя
- [ ] **Сохранение онбординга в БД** - сейчас генерируется план, но не сохраняется в `user_profile.preferences_json`
- [ ] **Сохранение настроек (имя, CEFR)** - сейчас только локально в state
- [ ] **API endpoint для обновления профиля** - `PUT /api/user/profile`
- **Файлы**: `App.tsx` (handleOnboardingComplete), `components/Views.tsx` (SettingsView), `api/app/routers/`

### 3. Проверка работы API
- [ ] **Тестирование всех эндпоинтов**:
  - `GET /api/courses` - возвращает курсы из БД
  - `GET /api/courses/{id}/lessons` - возвращает уроки
  - `GET /api/lessons/{id}` - возвращает вопросы
  - `POST /api/lessons/{id}/attempt` - сохраняет попытку
  - `GET /api/dashboard` - возвращает данные дашборда
  - `GET /api/progress` - возвращает прогресс
  - `POST /api/orchestrator/message` - работает с агентами
- [ ] **Проверить работу с реальной БД** (PostgreSQL или SQLite)

### 4. База данных
- [x] Миграции Alembic настроены
- [x] Seed скрипт заполняет курсы/уроки/достижения
- [ ] **Проверить, что seed выполняется без ошибок**
- [ ] **Проверить, что все 5 курсов с уроками загружены**

### 5. Переменные окружения
- [x] `.gitignore` настроен (`.env` не коммитится)
- [ ] **Создать `.env.example`** с примерами всех переменных
- [ ] **Проверить, что все переменные описаны в README**

## 🟡 Важно для стабильности (желательно перед запуском)

### 6. Интеграция фронтенда с бэкендом
- [x] `apiClient.ts` подключен к API
- [ ] **Убрать дублирование данных** - удалить старые данные из `constants.ts` (COURSES_DATA, ACHIEVEMENTS)
- [ ] **Проверить, что все данные загружаются с API**, а не из constants

### 7. Обработка ошибок
- [ ] **Обработка ошибок API** - показывать понятные сообщения пользователю
- [ ] **Обработка ошибок Gemini API** - fallback если API недоступен
- [ ] **Валидация данных** - проверка на фронте и бэке

### 8. Telegram интеграция
- [x] Webhook endpoint (`/api/telegram/webhook`)
- [x] Базовая команда `/start`
- [ ] **Проверить работу "Практика"** - выдача MCQ и сохранение в БД
- [ ] **Проверить синхронизацию** - прогресс из Telegram виден на Web

### 9. Orchestrator агенты
- [x] Промпты сохранены
- [x] GuardAgent, Router, TutorAgent, BuddyAgent, AssessorAgent, PlannerAgent реализованы
- [ ] **Протестировать все агенты** - проверить что они возвращают правильный JSON
- [ ] **Проверить GuardAgent** - блокирует инъекции (risk.blocked=true)

## 🟢 Желательно (можно после запуска)

### 10. Тестирование
- [ ] Unit тесты для API
- [ ] Integration тесты
- [ ] E2E тесты для основных сценариев

### 11. Мониторинг и логирование
- [ ] Настроить логирование (структурированные логи)
- [ ] Интеграция с Sentry или аналогом
- [ ] Метрики производительности

### 12. Безопасность
- [ ] Rate limiting для API
- [ ] CORS настроен правильно (не `allow_origins=["*"]` в production)
- [ ] Proxy для LLM ключей (не отправлять ключи на фронт)

### 13. Документация
- [x] README обновлен
- [ ] API документация (OpenAPI/Swagger)
- [ ] Инструкции по деплою

## 📝 Быстрая проверка перед запуском

Выполните эти команды:

```bash
# 1. Проверить, что backend запускается
cd api
source .venv/bin/activate
uvicorn main:app --reload --port 8000
# Проверить http://localhost:8000/api/health

# 2. Проверить, что frontend запускается
cd ..
npm run dev
# Проверить http://localhost:3000

# 3. Проверить, что seed работает
cd api
python -m app.seed
# Проверить, что нет ошибок

# 4. Проверить миграции
alembic upgrade head
# Проверить, что нет ошибок
```

## 🎯 Минимальный MVP для запуска

Для первого запуска достаточно:
1. ✅ Регистрация/логин работает
2. ✅ Курсы/уроки загружаются с API
3. ✅ Прохождение урока сохраняется в БД
4. ✅ Прогресс отображается на дашборде
5. ✅ Orchestrator возвращает JSON

Остальное можно доработать после запуска.
